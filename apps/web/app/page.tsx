"use client";

import Image from "next/image";
import { type CSSProperties, type FormEvent, type ReactNode, useEffect, useMemo, useRef, useState } from "react";

type Citation = { document_id: string; filename: string; score: number; source_url?: string | null };
type Message = { role: "user" | "assistant"; content: string; rewritten?: string; citations?: Citation[]; messageId?: string; feedbackState?: "fading" };
type LoginForm = { tenantSlug: string; username: string; password: string };
type TimingMap = Record<string, number>;

const apiBase = process.env.NEXT_PUBLIC_API_BASE_URL ?? "http://localhost:8000/api/v1";
const tokenStorageKey = "enterprise-rag-access-token";
const timingOrder = ["rewrite", "hyde_generation", "retrieval_rerank", "first_token", "generation", "cache_hit", "direct_answer_route", "total"];
const timingLabels: Record<string, string> = { rewrite: "Rewrite", hyde_generation: "HyDE", retrieval_rerank: "Retrieval / Rerank", first_token: "First token", generation: "Answer generation", cache_hit: "Cache hit", direct_answer_route: "Direct answer", total: "Total" };

function safeUrl(value: string) { try { const resolved = new URL(value, new URL(apiBase).origin); return resolved.protocol === "http:" || resolved.protocol === "https:" ? resolved.toString() : null; } catch { return null; } }
function formatDuration(value?: number) { return value === undefined ? "未触发" : `${(value / 1000).toFixed(value < 10 ? 4 : 2)} s`; }

function inlineMarkdown(text: string): ReactNode[] {
  const pattern = /(\[文档\s*(\d+)\]|!\[([^\]]*)\]\(([^)]+)\)|\[([^\]]+)\]\(([^)]+)\)|`([^`]+)`|\*\*([^*]+)\*\*)/g;
  const nodes: ReactNode[] = []; let cursor = 0; let match: RegExpExecArray | null;
  while ((match = pattern.exec(text)) !== null) {
    if (match.index > cursor) nodes.push(text.slice(cursor, match.index));
    if (match[2]) nodes.push(<sup className="document-citation" key={match.index}>{match[2]}</sup>);
    else if (match[3] && match[4]) { const url = safeUrl(match[4]); nodes.push(url ? <img className="markdown-image" key={match.index} src={url} alt={match[3]} loading="lazy" /> : match[0]); }
    else if (match[5] && match[6]) { const url = safeUrl(match[6]); nodes.push(url ? <a key={match.index} href={url} target="_blank" rel="noreferrer">{match[5]}</a> : match[5]); }
    else if (match[7]) nodes.push(<code key={match.index}>{match[7]}</code>);
    else if (match[8]) nodes.push(<strong key={match.index}>{match[8]}</strong>);
    cursor = pattern.lastIndex;
  }
  if (cursor < text.length) nodes.push(text.slice(cursor)); return nodes;
}
function MarkdownContent({ content }: { content: string }) {
  const lines = content.replace(/\r\n/g, "\n").split("\n"); const rendered: ReactNode[] = []; const code: string[] = []; let inCode = false; let list: string[] = [];
  const flush = (key: number) => { if (list.length) { rendered.push(<ul key={`list-${key}`}>{list.map((item, index) => <li key={index}>{inlineMarkdown(item)}</li>)}</ul>); list = []; } };
  lines.forEach((line, index) => { if (line.trim().startsWith("```")) { flush(index); if (inCode) { rendered.push(<pre key={`code-${index}`}><code>{code.join("\n")}</code></pre>); code.length = 0; } inCode = !inCode; return; } if (inCode) { code.push(line); return; } const item = line.match(/^\s*[-*]\s+(.+)$/); if (item) { list.push(item[1]); return; } flush(index); if (!line.trim()) return; const heading = line.match(/^(#{1,4})\s+(.+)$/); if (heading) { const tag = heading[1].length === 1 ? "h2" : heading[1].length === 2 ? "h3" : "h4"; const Tag = tag as "h2" | "h3" | "h4"; rendered.push(<Tag key={index}>{inlineMarkdown(heading[2])}</Tag>); return; } rendered.push(<p key={index}>{inlineMarkdown(line)}</p>); });
  flush(lines.length); if (inCode) rendered.push(<pre key="code-final"><code>{code.join("\n")}</code></pre>); return <div className="markdown-content">{rendered}</div>;
}

export default function HomePage() {
  const [messages, setMessages] = useState<Message[]>([]); const [query, setQuery] = useState(""); const [sessionId, setSessionId] = useState<string | null>(null); const [token, setToken] = useState<string | null>(null); const [authReady, setAuthReady] = useState(false); const [login, setLogin] = useState<LoginForm>({ tenantSlug: "default", username: "", password: "" }); const [authError, setAuthError] = useState(""); const [authBusy, setAuthBusy] = useState(false); const [busy, setBusy] = useState(false); const [uploading, setUploading] = useState(false); const [timings, setTimings] = useState<TimingMap>({}); const [pipelineStatus, setPipelineStatus] = useState("等待提问"); const [lyrics, setLyrics] = useState<string[]>([]); const [lyricLine, setLyricLine] = useState(0); const [lyricChar, setLyricChar] = useState(0); const [sendSplash, setSendSplash] = useState(false); const endRef = useRef<HTMLDivElement>(null); const followRef = useRef(true);
  const authHeaders = useMemo(() => token ? { Authorization: `Bearer ${token}` } : {}, [token]);
  const currentLyric = lyrics[lyricLine] ?? ""; const visibleLyric = Array.from(currentLyric).slice(0, lyricChar).join("");

  useEffect(() => { fetch("/content/lyrics.md").then((r) => r.ok ? r.text() : "").then((text) => setLyrics(text.split(/\r?\n/).map((line) => line.trim()).filter(Boolean))).catch(() => setLyrics([])); }, []);
  useEffect(() => { if (!lyrics.length) return; const done = lyricChar >= Array.from(currentLyric).length; const timer = window.setTimeout(() => done ? (setLyricLine((value) => (value + 1) % lyrics.length), setLyricChar(0)) : setLyricChar((value) => value + 1), done ? 500 : 110); return () => window.clearTimeout(timer); }, [lyrics, lyricLine, lyricChar, currentLyric]);
  useEffect(() => { const onScroll = () => { followRef.current = document.documentElement.scrollHeight - window.innerHeight - window.scrollY < 80; }; window.addEventListener("scroll", onScroll, { passive: true }); onScroll(); return () => window.removeEventListener("scroll", onScroll); }, []);
  useEffect(() => { if (busy && followRef.current) endRef.current?.scrollIntoView({ behavior: "auto" }); }, [busy, messages]);
  useEffect(() => { const stored = window.localStorage.getItem(tokenStorageKey); if (!stored) { setAuthReady(true); return; } fetch(`${apiBase}/security/me`, { headers: { Authorization: `Bearer ${stored}` } }).then((r) => { if (!r.ok) throw new Error("登录已过期"); setToken(stored); }).catch(() => window.localStorage.removeItem(tokenStorageKey)).finally(() => setAuthReady(true)); }, []);

  function clearAuthentication() { window.localStorage.removeItem(tokenStorageKey); setToken(null); setSessionId(null); setMessages([]); setTimings({}); setPipelineStatus("等待提问"); }
  async function authenticatedFetch(input: string, init: RequestInit = {}) { const headers = new Headers(init.headers); Object.entries(authHeaders).forEach(([key, value]) => headers.set(key, value)); const response = await fetch(input, { ...init, headers }); if (response.status === 401) clearAuthentication(); return response; }
  async function submitLogin(event: FormEvent) { event.preventDefault(); setAuthBusy(true); setAuthError(""); try { const response = await fetch(`${apiBase}/security/token`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ username: login.username, password: login.password, tenant_slug: login.tenantSlug }) }); if (!response.ok) throw new Error(response.status === 401 ? "用户名、密码或租户不正确" : await response.text()); const data = await response.json(); window.localStorage.setItem(tokenStorageKey, data.access_token); setToken(data.access_token); setLogin((value) => ({ ...value, password: "" })); } catch (error) { setAuthError(String(error)); } finally { setAuthBusy(false); } }

  async function submit(event: FormEvent) { event.preventDefault(); if (!query.trim() || busy || !token) return; const text = query.trim(); const history = messages.map(({ role, content }) => ({ role, content })); const assistantIndex = messages.length + 1; followRef.current = true; setMessages((current) => [...current, { role: "user", content: text }, { role: "assistant", content: "" }]); setQuery(""); setTimings({}); setPipelineStatus("正在准备"); setBusy(true); try { const response = await authenticatedFetch(`${apiBase}/chat/stream`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ query: text, history, session_id: sessionId }) }); if (!response.ok) throw new Error(await response.text()); if (!response.body) throw new Error("浏览器不支持流式响应"); const reader = response.body.getReader(); const decoder = new TextDecoder(); let buffer = ""; const apply = (event: Record<string, unknown>) => { if (event.type === "start") { setSessionId(String(event.session_id)); setPipelineStatus("正在处理"); } else if (event.type === "rewrite_delta") { setPipelineStatus("正在改写问题"); const value = String(event.content ?? ""); setMessages((current) => current.map((message, index) => index === assistantIndex ? { ...message, rewritten: (message.rewritten ?? "") + value } : message)); } else if (event.type === "rewrite") { setMessages((current) => current.map((message, index) => index === assistantIndex ? { ...message, rewritten: String(event.rewritten_query ?? "") } : message)); } else if (event.type === "timing") { const stage = String(event.stage ?? ""); if (stage) setTimings((current) => ({ ...current, [stage]: Number(event.duration_ms ?? 0) })); } else if (event.type === "delta") { setPipelineStatus("正在生成回答"); const value = String(event.content ?? ""); setMessages((current) => current.map((message, index) => index === assistantIndex ? { ...message, content: message.content + value } : message)); } else if (event.type === "done") { setPipelineStatus("处理完成"); setSessionId(String(event.session_id)); if (event.timings_ms) setTimings(event.timings_ms as TimingMap); setMessages((current) => current.map((message, index) => index === assistantIndex ? { ...message, rewritten: String(event.rewritten_query ?? message.rewritten ?? ""), citations: event.citations as Citation[], messageId: String(event.message_id) } : message)); } else if (event.type === "error") throw new Error(String(event.message ?? "流式请求失败")); }; while (true) { const { value, done } = await reader.read(); buffer += decoder.decode(value, { stream: !done }); const lines = buffer.split("\n"); buffer = lines.pop() ?? ""; lines.filter((line) => line.trim()).forEach((line) => apply(JSON.parse(line))); if (done) break; } if (buffer.trim()) apply(JSON.parse(buffer)); } catch (error) { setPipelineStatus("处理失败"); setMessages((current) => current.map((message, index) => index === assistantIndex ? { ...message, content: message.content || `请求失败：${String(error)}` } : message)); } finally { setBusy(false); } }

  async function upload(file: File) { if (!token) return; setUploading(true); const form = new FormData(); form.append("file", file); try { const response = await authenticatedFetch(`${apiBase}/documents/upload`, { method: "POST", body: form }); if (!response.ok) throw new Error(await response.text()); const result = await response.json(); alert(`入库完成：${result.filename}，${result.chunk_count} 个切片`); } catch (error) { alert(`上传失败：${String(error)}`); } finally { setUploading(false); } }
  async function feedback(messageId: string, value: number) { const reason = value === -1 ? window.prompt("请填写改进原因") ?? "" : ""; const response = await authenticatedFetch(`${apiBase}/messages/${messageId}/feedback`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ value, reason }) }); if (!response.ok) return; setMessages((current) => current.map((message) => message.messageId === messageId ? { ...message, feedbackState: "fading" } : message)); }

  if (!authReady) return <main className="auth-shell"><div className="auth-loading"><span className="brand-mark">W</span><p>Preparing workspace...</p></div></main>;
  if (!token) return <main className="auth-shell"><section className="auth-stage"><div className="auth-story"><div className="brand-lockup"><span className="brand-mark">W</span><span>Wa7er RAG</span></div><div className="auth-story-copy"><p className="eyebrow">Wa7er's SOUL</p><h1>Better than being each other's world,<br /><span className="universe-glow">be your own universe.</span></h1><p>Knowledge should feel clear, quiet, and close at hand.</p></div><p className="auth-footnote">Private knowledge workspace</p></div><form className="auth-card" onSubmit={submitLogin}><div className="auth-card-heading"><p className="eyebrow">ENTERPRISE RAG</p><h2>Sign in to your workspace</h2><p className="auth-help">Use your tenant account to continue.</p></div><div className="auth-fields"><label>Tenant<input value={login.tenantSlug} onChange={(event) => setLogin({ ...login, tenantSlug: event.target.value })} required /></label><label>Username<input value={login.username} onChange={(event) => setLogin({ ...login, username: event.target.value })} autoComplete="username" required /></label><label>Password<input type="password" value={login.password} onChange={(event) => setLogin({ ...login, password: event.target.value })} autoComplete="current-password" required /></label></div>{authError && <div className="auth-error">{authError}</div>}<button className="primary-button" type="submit" disabled={authBusy}>{authBusy ? "Signing in..." : "Sign in"}</button></form></section></main>;

  return <main className="app-shell"><header className="topbar"><div className="brand-lockup"><span className="brand-mark">W</span><span>Wa7er RAG</span></div><div className="topbar-custom"><span className="topbar-custom-mark">R</span><span className="topbar-custom-text">{visibleLyric}<span className="lyric-caret" /></span></div><div className="header-actions"><label className="upload secondary-button"><span>{uploading ? "Uploading..." : "Upload"}</span><input type="file" accept=".docx,.pdf,.md,.markdown,.txt,.html,.htm" disabled={uploading} onChange={(event) => event.target.files?.[0] && upload(event.target.files[0])} /></label><button className="text-button" onClick={clearAuthentication}>Sign out</button></div></header><section className="workspace-heading"><p className="eyebrow">By Miao Chen</p><h1>Wa7er's Planet</h1><p>Only then will you stop treating your growth as an accident.</p></section><div className="conversation-layout"><div className="conversation-main"><section className="chat" aria-live="polite">{messages.length === 0 && <div className="empty"><div className="empty-symbol"><Image src="/logo.png" alt="Wa7er" width={74} height={74} priority /></div><p className="eyebrow">Start a soulful Q&amp;A session.</p><h2>Your question?</h2><p>You can ask something that touches the soul.</p><div className="prompt-examples"><button type="button" onClick={() => setQuery("How do I configure the system?")}>Freedom</button><button type="button" onClick={() => setQuery("What should I pay attention to?")}>Faith</button><button type="button" onClick={() => setQuery("Help me find the relevant document.")}>Solitude</button></div></div>}<div className="message-list">{messages.map((message, index) => <article className={`message ${message.role}`} key={`${message.role}-${index}`}><div className="message-meta"><span className="avatar">{message.role === "user" ? "You" : "R"}</span><span className="role">{message.role === "user" ? "You" : "Wa7er"}</span></div><div className="message-body">{message.rewritten && <div className="rewrite"><span>Query rewrite</span>{message.rewritten}</div>}<MarkdownContent content={message.content} />{message.citations?.length ? <div className="source-block"><p>Sources</p><div className="citations">{message.citations.map((citation, citationIndex) => { const url = citation.source_url ? safeUrl(citation.source_url) : null; const label = `文档${citationIndex + 1}`; return url ? <a key={citation.document_id} href={url} target="_blank" rel="noreferrer"><span>{label} · {citation.filename}</span><small>{citation.score.toFixed(3)}</small></a> : <div className="citation-item" key={citation.document_id}><span>{label} · {citation.filename}</span><small>{citation.score.toFixed(3)}</small></div>; })}</div></div> : null}{message.messageId && <div className={`feedback ${message.feedbackState === "fading" ? "fading" : ""}`}><span>Was this helpful?</span><button type="button" onClick={() => feedback(message.messageId!, 1)}>Helpful</button><button type="button" onClick={() => feedback(message.messageId!, -1)}>Needs work</button></div>}</div></article>)}</div><div ref={endRef} className="stream-anchor" /></section><form className="composer" onSubmit={submit}><div className="composer-field"><textarea value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Enter a prompt…" rows={2} /><span>The answer will be provided based on the database.</span></div><button className="send-button" type="submit" disabled={busy || !query.trim()} aria-label="Send"><span className={query ? "send-wave active" : "send-wave"} style={{ "--send-fill": `${Math.min(Array.from(query).length, 10) * 10}%` } as CSSProperties} /><span className="send-button-content">↑</span></button></form></div><aside className="timing-panel" aria-live="polite"><div className="timing-panel-heading"><div><p className="eyebrow">Reply Metrics</p><h2>Pipeline</h2></div><span className={`timing-status ${busy ? "active" : ""}`}>{pipelineStatus}</span></div><div className="timing-table">{timingOrder.map((stage) => <div className={`timing-row ${stage === "total" ? "total" : ""}`} key={stage}><span>{timingLabels[stage]}</span><strong>{formatDuration(timings[stage])}</strong></div>)}</div><p className="timing-note">Each response records the time spent across the retrieval pipeline.</p></aside></div></main>;
}