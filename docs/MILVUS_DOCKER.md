# Milvus Docker ??

???? Milvus `v2.6.19` Standalone?? Docker Compose ???? etcd ? Milvus ?? MinIO?

## ??

???? Docker Desktop???????????? `cmd.exe` ????

```bat
scripts\start_milvus.cmd
```

?????

- Milvus gRPC?`localhost:19530`
- Milvus ????????`localhost:9091`
- Milvus ?? MinIO API?`localhost:9010`
- Milvus ?? MinIO Console?`localhost:9011`

?????

```bat
curl http://localhost:9091/healthz
```

## ????

?? `.env.example` ? `.env`??????

```env
RAG_USE_MOCKS=false
MILVUS_URI=http://localhost:19530
MILVUS_COLLECTION=enterprise_knowledge
RAG_EMBEDDING_DIMENSION=1024
```

Milvus Collection ?? 1024 ? `FLOAT_VECTOR`?COSINE ??? HNSW ?????? `M=16`?`efConstruction=256`????? `ef=64`??????????? Collection ???????????????????????????

## ??

```bat
scripts\stop_milvus.cmd
```

????????????????????????? `--volumes` ? Compose ???
